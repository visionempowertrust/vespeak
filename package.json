{
  "name": "vespeak",
  "version": "1.0.0",
  "private": true,
  "description": "Voice-first field feedback collection with Sarvam AI transcription",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=18"
  }
}
